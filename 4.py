#write a program that accepts a sequence of numbers and outputs a list and a tuple with those items
#split ',' splits each number by a comma. 
numbers = list(input('enter numbers: ').split(','))
print(numbers)

print(tuple(numbers))

